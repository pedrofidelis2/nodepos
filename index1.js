import { obterTodos, criarProduto, alterarProduto, zerarQtd, removerProduto} from "./controller-produto.js";

//obterTodos().then((x) => console.log(JSON.stringify(x)));

//obterTodos().then((x) => {
    //for( let obj of x)
        //console.log(JSON.stringify(obj));
//});

//criarProduto("Manga", 700).then((x) => console.log(`Id gerado: ${x}`));

//alterarProduto(2, 'Abacate', null).then((x) => console.log(JSON.stringify(x)));

//zerarQtd().then((x) => console.log(`Linhas atualizadas: ${x}`));

//removerProduto(4).then((x) => console.log("Produto removido com sucesso!"));